# ─── Vite Configuration ──────────────────────────────────────
# Backend API URL (used in production build, not needed in dev with proxy)
# VITE_API_URL=http://localhost:5000

# The dev server port
# VITE_PORT=3000
